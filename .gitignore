__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.DS_Store
.claude/settings.local.json

# OC22 bulk downloads (500+ MB); oc22_survey.py documents how to re-fetch
data/oc22/*.pkl
data/oc22/*.tar.gz
data/oc22/is2res_total_train_val_test_lmdbs/

# Run artifacts (records + structures written to disk)
runs/
*.traj
*.log

# Local model checkpoints (56 MB-2+ GB each); backends.py's MODEL_DIR default points here.
# Re-stage per-model: MACE/Orb from their own public URLs, UMA/UMA-M are HF-gated (accept
# the FAIR Chemistry License, download by hand).
models/
